{
  "name": "@wgr-sa/strapi-provider-upload-open-stack",
  "version": "4.11.4",
  "description": "OpenStack provider for strapi upload",
  "keywords": [
    "upload",
    "open stack",
    "OpenStack",
    "strapi"
  ],
  "main": "./dist/src/index.js",
  "types": "./dist/src/index.d.ts",
  "files": [
    "./dist"
  ],
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w --preserveWatchOutput",
    "clean": "run -T rimraf ./dist",
    "prepublishOnly": "tsc",
    "test:unit": "run -T jest",
    "test:unit:watch": "run -T jest --watch",
    "lint": "run -T eslint ."
  },
  "dependencies": {
    "@strapi/logger": "^4.13.7",
    "axios": "^0.27.0",
    "streamifier": "^0.1.1"
  },
  "engines": {
    "node": ">=14.19.1 <=24.12.0",
    "npm": ">=6.0.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.2",
    "tsconfig": "^7.0.0",
    "typescript": "^5.1.6"
  }
}
